# Laufzeit-Daten (pro PC, gehören nicht ins Repo)
/logs/
/backups/
/reports/

# Gemerkte Einstellungen (Sprachwahl) — reisen mit dem Stick, nicht mit dem Repo
/einstellungen.json

# Offline-Cache (Installer, ODT, Office-Payload) — kann mehrere GB werden
/offline/*
!/offline/.gitkeep

# Gebaute Release-Archive (tools\New-Release.ps1)
/dist/

# Ergebnisse der Sandbox-Selbsttests (tools\Test-Sandbox.wsb)
/sandbox-ergebnis/

# Windows
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# Editor
.vscode/
*.swp
